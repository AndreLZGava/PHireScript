Run the PHireScript unit test suite with PHPUnit.

From your root sandbox project

```bash
cd /phirescript && vendor/bin/phpunit
```

Report which tests passed and which failed. If any test fails, show the failure message and the relevant test file.
