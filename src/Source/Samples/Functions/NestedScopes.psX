item = "global"

outer() {
    item = ["list"]

    inner() {
        item = 10
        item.display!()
    }

    item.add("nested")
    inner()
}

config = {
    user: {
        name: "André",
        id: 1
    },
    active: true
}

echo item.toUpperCase()        // STRING
